# Skills

Each reusable workflow belongs in its own `skills/<skill-name>/` directory.
Keep skill-specific instructions and policy assets with that skill. Shared code
and third-party Python dependencies belong in the root `runtime/` directory and
`requirements.txt`.
