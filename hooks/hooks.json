{
  "description": "Wttch 私人插件共享的 Hook。",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${PLUGIN_ROOT}/runtime/bootstrap.py ${PLUGIN_ROOT}/runtime/model_gate.py",
            "timeout": 10,
            "statusMessage": "正在准备 Python 环境"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|apply_patch|Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${PLUGIN_ROOT}/runtime/bootstrap.py ${PLUGIN_ROOT}/runtime/jev_gate/main.py jev-gate --policy ${PLUGIN_ROOT}/skills/jev-gate/gate.yml",
            "timeout": 30,
            "statusMessage": "正在准备 Python 环境"
          }
        ]
      }
    ]
  }
}
